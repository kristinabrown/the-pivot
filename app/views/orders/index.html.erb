<div class="container">

  <h1>Past Orders</h1>
  
  <div class="stitched">
    <table>
      <tr>
        <th> Item </th>
        <th> Order Total</th>
      </tr>

        <% @orders.each do |order| %>
        <tr>
          <td><%= order.item.name %> </td>

          <td><%= number_to_currency(order.total) %></td>
        </tr>
        <% end %>

    </table>
  </div>

</div>
