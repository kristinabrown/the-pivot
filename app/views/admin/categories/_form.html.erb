<div class="container">
<div class="stitched">
  <%= form_for [:admin, @category] do |f| %>
    <%= f. label :name %>
    <%= f.text_field :name %>

    <%= f. label :description %>
    <%= f.text_field :description %>

    <%= f.submit %>
  <% end %>
</div>
</div>
